# 修订记录 2026-09-06

## 每日挑战：题库 16 → 40 题，顺带修掉两个会白干的前提

用户拍板「先扩题库，手记留着」。扩的时候发现两个前提问题，不修的话新题白写：

### 1. `CHALLENGE_KEYS` 写死 7 味 → 改为从 ACCORDS 派生（12 味）
- **原状**：`['citrus','floral','fruity','woody','oriental','fougere','green']`，
  后加的香草 / 烟草 / 水生 / 琥珀 / 麝香**不在这个列表里**。
- **后果**：一旦题目 target 写了这些键，评分的余弦相似度根本不遍历它们——
  用户照提示调了香草，分数不认。
- **改法**：`const CHALLENGE_KEYS = ACCORDS.map(a => a.key)`，以后加香调自动跟上。
  参照向量 NEUTRAL 同步从「七味各 1」变「十二味各 1」（工坊滑块本来就是 12 根，参照更准）。

### 2. 取题按「几号」取模 → 改为按「一年里的第几天」
- **原状**：`day % DAILY_CHALLENGES.length`，day 取日期的「日」，范围只有 1–31。
- **后果**：题库只要超过 31 条，排在后面的题**永远轮不到**，而且每月都从同一批开始。
  照旧逻辑扩到 40 题，会有 9 题是死题。
- **改法**：按年内第几天（1–366）取模，跨月不重置。实测一年 366 天：
  **40 题全部被取到，单题出现 9~10 次**（旧逻辑一轮只覆盖 31 题）。

### 3. 新题补齐两类空白
- **五个后进香调首次进题库**（原 16 题一道都没出过）：
  香草 ×7、麝香 ×11、琥珀 ×7、水生 ×5、烟草 ×4。
- **老七调新搭配**：黄油曲奇 / 焦糖布丁脆壳 / 烟斗 / 雪茄 / 泳池 / 海雾 / 蜜蜡旧信 /
  烘干机刚停下 / 中药铺抽屉 / 冰镇西瓜 / 干草垛午睡 / 雪后松林 等。
- 主题无重复、target 键全部合法、12 香调**无遗漏覆盖**。

## 满分 95 → 99（正解留一分给明天），并收敛为常量

用户定调：正解就是 99，最后一分永远留给明天的题。

- `mix.js` 新增 `export const CHALLENGE_MAX = 99` 与 `CHALLENGE_MIN = 10`（分数地板）。
  评分改为 `10 + norm × (99 - 10)`，正解恒 99、纯水恒 10。
- **为什么要常量**：这个值历史上改过 100 → 95 → 99，每次都要翻七八个文件改字面。
  现在 7 个文件的显示统一从常量取——lab 横幅/吸顶/弹窗、home 回显、community 按钮、
  card 对决与分享标题、canvas 分享图、彩蛋 perfect 判定、测试用例。
- 文案同步：「满分 95…剩下的 5 分」→「满分 99…剩下的 1 分」，分母用 `100 - CHALLENGE_MAX` 算，
  改常量时文案自动跟。eggs.js 彩蛋描述改成「顶到评分上限…永远给明天留一分」，不再写死数字。

### 验证
| 项 | 结果 |
|---|---|
| 题库 | 40 题，键合法、主题无重复、12 香调全覆盖 |
| 轮转覆盖 | 一年内 40/40 题被取到，单题 9~10 次 |
| 评分语义 | 正解 99 / 纯水 10 / 随手乱调 10 / 正解掺无关香调降至 59（惩罚生效） |
| 功能测试 | 117 条全绿 |
| audit 门禁 | 0 失败 |
| build | DONE，主包 1305KB |

## AI 评香：封存卡「古先生说」升级为按配方生成的针对性评论

用户需求：接入 AI 针对性评论，替换封存卡上随机抽的「古先生说」句。选择
交互策略「等 AI 出评论再出卡」，通道做成 OpenAI 兼容可配（默认大赛 Coding Plan）。

### 架构
- **cloudfunctions/aiQuote/（新云函数）**：Node 原生 https POST /chat/completions，零依赖。
  lib.js 抽纯函数（buildMessages/sanitizeQuote/hasBannedWord，CJS 无依赖可单测）；
  index.js 读环境变量 AI_BASE_URL（默认 https://chatapi.weixin.qq.com/openai/v1）/
  AI_API_KEY/AI_MODEL（默认 DeepSeek-v4-flash），未配 key 返回 NO_AI_KEY。
  换 DeepSeek 官方等通道只改环境变量。⚠️ 部署后云函数超时调到 ≥10s。
- **src/utils/ai-quote.js（前端）**：buildAiQuotePayload 纯函数（12 香调→非 0 项
  label+pct，name 截 20）+ getAiQuote：判 wx.cloud 存在、3.5s 超时、任何失败 resolve(null)。
- **lab.vue sealCore**：出码与 AI 评香 Promise.all 并行（loading 一段），AI 非空才覆盖
  quote.value（AI 版定稿），空则本地台词池原句。只对非纯水调 AI（留白句设计好不让模型抢话）。

### persona / 防 AI 味（lib.js SYSTEM_PROMPT + 双闸）
- 提示词：古先生口吻，只输出一句 20~50 汉字，针对配方不许万金油，禁升华。
- sanitizeQuote：迭代剥「外层引号」与「古先生说：」前缀（两种顺序都兼容）、剥首尾标点、
  超 52 字在句内断点截断、<6 字判敷衍返回 null。
- hasBannedWord 第二道闸：命中仿佛/氤氲/缱绻等 27 个书面词直接拒（回退本地池，不重试省配额）。

### 降级语义（用户明确要求：AI 出错用既有文案）
AI 超时/未部署/没配 key/上游错误/命中禁词 → null → quote 保持 GU_QUOTES 随机池原句；
封存主流程对 AI 零依赖，不卡顿不出白卡；旧卡（quote 已持久化）不动。

### 验证
功能测试 117 → **128 条全绿**（新增 cases-ai-quote.mjs 11 条：载荷构造/prompt/
清洗含两种前缀顺序/52 字截断/过短拒/禁词）；audit 0 门禁；build DONE 主包 1306K（+1K）；
dist/cloudfunctions 自动含 aiQuote。

### 踩坑
- 测试框架 expect 无 toContain/toBeLessThanOrEqual，改用 indexOf/length 断言。
- 「，这支香挺冲」=5 字会触发 <6 拒（用例字数数错，非实现 bug）。
- 前缀正则首版缺「说」字且被引号包裹时不在行首 → 改迭代剥 + 古先生(说|曰)?。

## AI 评香体验微调：封存不等 AI（缓存 + 缩短超时）
用户问「封存会不会太慢」——最坏情况 AI 3.5s 超时会让封存从 ~2s 拉到 3.5s 干等。两个改动：
1. **AI 结果 LRU 缓存**（ai-quote.js）：同配方+同香名的评论只调一次 AI（storage 键
   isabella_ai_quote_cache，cap 30 删最旧；sig=12 味逗号串+香名，改名会换签名重新生成）。
   重封同一瓶 / 收藏重开 → 缓存命中，几乎零延迟。
2. **超时 3.5s → 2.5s**（AI_QUOTE_TIMEOUT=2500）：宁可回退本地 120 句文案库，也不让用户干等。
新测试 3 条（超时上限 ≤2.5s / sig 稳定且含名 / LRU 淘汰），功能测试 128→131 全绿。

## 图鉴合规：4 张名香瓶图替换为 Wikimedia Commons CC 许可图 + 署名落地

用户选项「逐款找 CC 许可图」。用 Wikimedia Commons API（带 UA、429 退避重试）
对 16 款做两轮精准搜索，结果诚实：

**找到可用（4 款，目检确认真瓶图）→ 已替换为本地 + 详情页加署名**

| id | 香水 | 图源 | 许可 | 落地 |
|---|---|---|---|---|
| 1 | 尼罗河花园 | Hermès Jardin sur le Nil.jpg (Wikimedia Commons / Kiêu Anh-Tran) | CC BY 3.0 | ✓ 替换，详情页署名 |
| 3 | 蝴蝶夫人 | Mitsouko.jpg (Wikimedia Commons) | CC BY-SA 2.0 | ✓ 替换，详情页署名 |
| 7 | 香奈儿 5 号 | Chanel - Flickr - Kevin Rheese.jpg | CC BY 2.0 | ✓ 替换，详情页署名 |
| 8 | 大地 | Terre d'Hermès (cropped).JPG (Wikimedia Commons / IDiamond) | CC BY-SA 3.0 | ✓ 替换，详情页署名 |

**找不到（11 款，Commons 没有自由许可瓶图）→ 保留现状，列名存档**

| id | 香水 | 原因 |
|---|---|---|
| 2 | 檀道 | 0 候选 |
| 4 | 孤女 | 0 候选 |
| 5 | 大吉岭茶 | 候选是另一款 Bvlgari AQVA/合集 |
| 6 | 黑色圆舞曲 | 中文昵称→英文名不可考，0 候选（**该款数据存疑：data.js brand=阿蒂仙，但阿蒂仙产品线未见此名**） |
| 9 | 真我 | 0 候选 |
| 10 | 英国梨与小苍兰 | 0 候选 |
| 11 | 柏林少女 | 0 候选 |
| 12 | 天使 | 命中全是摇滚乐队 "The Phenomenauts" |
| 13 | 冷水 | 候选是 "Cool cool water" Flickr 标题（实际是条狗） |
| 14 | 一千零一夜 | 命中全是克什米尔/德里 Shalimar 花园 |
| 15 | 蔚蓝 | 命中是 1920 年代 Coco Chanel 画作 |

**合规落地**（替换只是表面）
- data.js 加 imageCredit 可选字段 `{ text, license, url }`（仅这 4 款有）
- 详情页加 .d-credit 极小字（20rpx 灰，链接到原图 Wikimedia File 页）——CC BY/SA 必署名
- openCreditUrl：小程序环境用 setClipboardData 复制链接（不弹浏览器）

**现实结论**：商业香水瓶的自由许可照片本来就稀缺 + 标题噪声大；能换 4 款是性价比最高的极限。
未覆盖 11 款仍用现图（用户自搜，授权不明），按先前"现状过渡"策略：**提报材料与宣传不用实拍瓶图**，
小程序内继续跑。上架前再决定是 B（自摄）还是继续凑 CC。

**额外发现**：`data.js` 里 id=6「黑色圆舞曲/阿蒂仙/2009/木质」这条，Commons 找不到对应英文名，
搜索阿蒂仙产品线（1978~2019）也未见此名——**这条数据本身可能存疑**（可能是早期昵称录入或误对应）。
建议有空时确认英文原名或从图鉴里临时降权。
