# 修订记录 · 2026-09-04

## 前中后调：三调行加「层占比」，层不再整层蒸发

### 背景（用户反馈）
工坊三调行 / 封存卡金字塔「调了之后有的不展现中调/后调」。用真实香料库模拟证实根因：

- 归层链路是 `滑块 12 香调% → generateFormula 前 6 味香料 → 每味按主导香调归层`，
  三调只是**名字榜**，不是**占比榜**。
- 柑橘系香料在库内排最前且普遍带双系加成（苦橙 citrus.88+woody.12），同分/近分全占前六；
  实测 `柑橘 50 + 木质 50` 前六全是柑橘系 → **后调整层消失**；`木质 70 + 花香 20 + 麝香 10` 也只剩后调一行。
- 层里没有代表香料上榜 → 该行被 pyramidRows 过滤掉，用户以为是「没调出来」，其实是「名字没上榜」。

### 改动（commit 待填）
- `src/utils/mix.js`：新增导出 `tierRatio(accordValues)` —— 12 香调按
  PYRAMID_TIER 三层加总，最大余数法摊成和为 100 的整数（只看香调，SOLVENT 不进结构；
  纯水/全 0 返回 null）。与 generatePyramid 分工：那边列名字、这边报占比。
- `src/pages/lab/lab.vue`：
  - 导入 tierRatio，新增 `tierPct`（computed，由 reactive values 派生 → 拖动时与滑块同帧更新）；
  - `pyramidRows` 由「过滤空层」改为「有香调即三层常显」，每行带 `ratio`；
  - 模板三调行标签后加 `{{row.ratio}}%`，0 层加 `.zero` 淡色弱化（0 也有信息：这层还没加）；
  - 纯水（tierRatio=null）仍整块隐藏，不硬凑三行 0；新增 `.pyr-pct` / `.pyr-pct.zero` 样式。
- 封存卡 / 历史 / 收藏链路不动（卡面保持简洁名字行，属既有设计）。
- `scripts/func/cases-mix.mjs`：新增「前中后层占比 tierRatio」suite 5 条
  （null 边界 / 中调如实 0 / 求和 100 / SOLVENT 无关 / 单层 100）。

### 验证
- 功能测试 103 → **108 条全绿**；`npm run audit` 提醒级 0 门禁失败；
- `build:mp-weixin` DONE，主包 1017K；产物含 `tierRatio`、wxml `{{row.b}}%` 与 zero 类绑定。

---

## 前中后调：三调行加 ⓘ 释义弹层（解释 + 分类）

### 背景（用户反馈）
三调行有了层占比后，用户进一步要「前/中/后」标签旁像香调滑块一样有个小感叹号，
点进去能看这一层是什么 + 这一层收着哪些香调（分类）。

### 改动（commit 待填）
- `src/utils/data.js`：
  - `PYRAMID_TIER`（12 香调 → top/middle/base 映射）从 mix.js 上收为**唯一事实源**并导出；
    注释写明禁止别处再抄一份（曾双份维护漂移）。
  - 新增导出 `PYRAMID_TIERS`：三层各自的 label/tagline/description/note 文案
    （口吻延续 ACCORDS：白话 + 小比喻，不写古先生；tagline 进弹窗标题）。
- `src/utils/mix.js`：删除本地 `PYRAMID_TIER` 重复定义，改从 data.js import；
  新增导出 `tierAccords(tierKey)` —— 按 ACCORDS 顺序反向聚合该层香调 key，
  层释义的「分类」与三调行/层占比永远同源。
- `src/pages/lab/lab.vue`：
  - `pyramidRows` 每行补 `key`；三调行标签内嵌小圆 `i`（`.pyr-info`，30rpx，
    与香调滑块 `.slider-info` 同语言：小圆 = 可点开解释）；
  - 新增前中后调释义 sheet：`标题「前调 · 最先开口，也最先离开」/ description / note
    （.sheet-note 灰底引用）/ 分类 chips「这一层收着这些香调」/ 知道了按钮`；
  - 分类 chips 文字色走各香调 `accordTextColor`（深色版），点 chip **递归切入**该香调的
    单独释义 sheet；两个底 sheet 互斥（openAccordDesc 与 openTierDesc 互相清对方），绝不叠开；
  - 新增 `.pyr-info` / `.tier-chip`（中性描边，色字表身份）/ `.sheet-sub-tip` 样式。
- `scripts/func/cases-mix.mjs`：新增「层分类 tierAccords」suite 4 条
  （三层收调正确 / 12 香调无重叠无遗漏 / TIERS 与映射同源 / 文案齐全非空）。
  修正：base 期望顺序按 ACCORDS 真实序 `woody/oriental/musk/amber/...`（musk 在 amber 前）。

### 验证
- 功能测试 108 → **112 条全绿**；`npm run audit` 提醒级 0 门禁失败；
- `build:mp-weixin` DONE，主包 1017K；产物：wxml `pyr-tag` bindtap 内嵌 `pyr-info` 小圆、
  sheet「这一层收着这些香调」、wxss `.pyr-info/.tier-chip`、data chunk 双导出齐全。

---

## 「我的」页挑战卡补完成态回显 + 历史/收藏空态 CTA（第二批建议 1、2）

### 1 · 挑战卡完成态（community.vue）
- 模板：卡片 hint 按 `dailyDone` 切换「明天换新题 · 再来练手 / 根据标题推测香调，来试试」；
  按钮由死文案「接受」变 `dailyDone ? (challengeScore != null ? '✓ X/95' : '✓ 已完成') : '接受'`，
  与首页卡片同口径（首页是「✓ 今日 X/95」，这里按钮位窄取「✓ X/95」）。
- `challenge` 由 computed 改为 ref + 每次 onShow 重读：computed 无响应依赖只会算一次，
  页面跨天存活时会一直显示昨天的题；现在 getDailyChallenge()/isChallengeDone()/getChallengeScore()
  每次进页刷新，跨天换题、完成态归零都即时。
- 点按钮仍走原 acceptChallenge：已完成者先弹「今日已完成 / 只有分数更高才更新」确认。

### 2 · 空态 CTA（history.vue / favorites.vue）
- 两页空态由一句不可点文字 → 垂直居中块：正文 + 「去工坊调一瓶」深绿按钮，
  `goLab()` = `track('empty_go_lab')` + `uni.switchTab({ url: '/pages/lab/lab' })`
  （工坊是 tabBar 页，必须 switchTab）。
- history 正文微调避免与按钮重复词；顺带清理 history.vue `.empty` 样式重复定义两遍的残留
  （199/205 行），两页 `.empty` 统一为 flex 居中容器 + `.empty-btn`。

### 验证
- 功能测试 112 条全绿；`npm run audit` 提醒级 0 门禁失败；`build:mp-weixin` DONE，主包 1017K；
- 产物核对：community.wxml 挑战卡插值（编译压为 `{{e}}/{{f}}/{{g}}`）、community.js 含
  「明天换新题 · 再来练手」+ `✓ ` ×2 + 三个挑战函数各 2 次调用；两页 wxml/js/wxss 均有
  `empty-btn / 去工坊调一瓶 / empty_go_lab`。

---

## 图鉴金字塔补齐：香水层占比 + 手记/香水 ⓘ 释义（第 3 条）

### 背景（用户拍板落地）
第二批建议第 3 条：图鉴侧「前中后调」只有工坊有，图鉴两处金字塔（香水详情无金字塔、
手记详情只有名字行）都没有 ⓘ，用户逛图鉴撞见「前调」却没法问「这是什么」。

### 改动（gallery.vue）
- **香水详情**：dataOpen「香气结构」区内、色带前新增金字塔白卡三行 ——
  `tierRatio(sel.data.accords)` 现算层占比（图鉴名香没有官方香料名单，那份在手记里，
  这里给「前 46 · 中 30 · 后 24」结构视角），行内 `.pyr-pct` 金色读数；
- **手记详情**：既有官方香料名单金字塔三行，行标签包一层 `.pyr-info` 小圆 i（30rpx，
  与 lab 同款），点击整行标签可点；
- **共用层释义 sheet**：`tierKey` ref → `PYRAMID_TIERS.find` 取 label/tagline/description/note
  （与 lab 三调行 ⓘ 完全同源文案）；分类 chips 由 `tierAccords(tierKey)` 派生；
  sheet 与 radarHelpOpen 各自独立开关；
- 样式：`.pyr-label` 70→110rpx（容纳「前调+ⓘ」）、新增 `.pyr-info/.pyr-pct/.sheet-desc/
  .sheet-sub/.sheet-sub-tip/.chip-row/.chip/.tier-chip`（照搬 lab 同款，scoped 独立）。

### 验证
- 功能测试 112 条全绿；`npm run audit` 提醒级 0 门禁失败；`build:mp-weixin` DONE，主包 1017K；
- 产物：gallery.wxml `pyr-info`×4（香水 v-for 1 + 手记三行）、香水 `pyr-pct`、
  sheet「这一层收着这些香调」、三处 `pyr-label` 均带 bindtap；gallery.js 含
  PYRAMID_TIERS/tierAccords/tierRatio；wxss 样式齐全。

---

## 修正：层释义里点分类香调 = 额外弹一层释义 sheet（不跳详情页）

### 背景（用户拍板修正）
上节初版把层释义的分类 chip 接成 `goTierAccord` → `openAccord` **直接翻到该香调的
详情页**（当时理由是图鉴本就有香调详情页、顺带计入翻阅计数）。用户看后拍板：
**「4号小设计是错的，就是额外的弹一层」** —— 要的是像工坊那样从层释义递归弹出一张
香调释义 sheet，而不是翻页离开当前上下文。

### 改动（gallery.vue）
- 删除 `goTierAccord`（跳详情页行为）；新增叠层 sheet 状态与函数：
  `accordSheetKey / accordSheetInfo（ACCORDS.find）/ openAccordSheet(k) / closeAccordSheet()`；
- 层释义 sheet 的分类 chip `@tap` 改绑 `openAccordSheet(k)`，**tierKey 不清** —— 香调释义
  sheet 盖在层释义 sheet 之上（真正的「再弹一层」），关掉它回到层释义，再关回详情；
- 香调释义 sheet 内容照 lab 同款：标题「柑橘 · 这是什么香」/ description /
  常见原料 chips（accordTextColor 上色）/ 知道了；空 `typicalIngredients` 不占位；
- 顶层 sheet 样式：`.sheet-mask.top { z-index:300 }` / `.sheet.top { z-index:301 }`，
  盖过底层 sheet（200/201），教程 CoachMask 仍最顶（2000）；
- 香调**完整详情页**入口保持首屏香调卡（line 61 `openAccord`），弹层只解释不跳页；
  提示文案「点一下看它的单独介绍」→ 与工坊同款「点一下看它的单独释义」。
- 注：原 goTierAccord 附带「翻阅计数」随之取消 —— 弹层看释义不算翻页（只有真正进
  香调详情页才计 seen），语义更贴「卷末余香」彩蛋本意。

### 验证
- 功能测试 112 条全绿；`npm run audit` 提醒级 0 门禁失败；`build:mp-weixin` DONE，主包 1017K；
- 产物：gallery.wxml 含顶层 `sheet-mask top`/`sheet top` +「这是什么香」标题；
  `goTierAccord` 零残留；wxss `.sheet-mask.top/.sheet.top` 存在。

