# 修订记录 · 2026-09-03

> 本轮为「全项目系统性自检 → 一一清理」的完整修订存档。所有改动均 src + dist 双写同步，
> dist 为手写补丁（split/join + 锚点计数守卫），29+1 处全部命中，node --check 全过，
> favorites / archive / wxacode LRU 三个模块另跑了沙盒功能测试验证。

## 一、P1（功能性 bug，2 项）

### P1-1 收藏/历史卡片画不出三调
- 位置：`src/pages/card/card.vue`（onLoad data 组装 ~:174、toggleFav 条目 ~:437）
- 原因：从收藏页、历史页打开的卡片，data 与收藏条目都没带 pyramid 字段，drawCard 拿不到三调数据。
- 修复：两处补 `pyramid: ... || null`（画卡与测高对空 pyramid 自跳过，扫码/分享卡不受影响）。
- dist：`dist/pages/card/card.js` 两处 `pyramid:n.pyramid||null` / `pyramid:d.value.pyramid||null`，语义一致。

### P1-2 收藏「假反馈」
- 位置：`src/utils/favorites.js`（toggleFav）、`src/pages/history/history.vue`（fav）、`src/pages/card/card.vue`（toggleFav）
- 原因：toggleFav 写失败或入参非法时返回 false，调用方按「已取消收藏」提示——数据还在，用户被误导。
- 修复：契约改三态 **true=已收藏 / false=已取消 / null=写失败或非法入参**（`!item||!item.time → return null`）；history 与 card 收到 null 时 toast「收藏没存上，请重试」且界面状态不动。
- dist：favorites.js `return null`、history.js fav 分支、card.js toast 分支均已同步。

## 二、P2（边界/体验，9 项）

1. **收藏条目字段缺失**：favorites.js 条目补 `pyramid`/`radarMode`；history.vue、favorites.vue 的 openCard 暂存同步补齐——收藏卡/历史卡打开时雷达模式与三调不丢。
2. **removeFav 假成功**：`src/utils/favorites.js` removeFav 改为 `return save(list)` 返回真实写入结果；favorites.vue 取消收藏（unfav）与长按删除（del）失败时 toast「没存上，请重试」，不更新列表。
3. **小程序码缓存无限增长**：`src/utils/wxacode.js` 新增 LRU 清理 trimWxacodeCache——索引键 `wxacode:index`，上限 20，淘汰时 removeStorageSync + fs.unlink；空索引用 getStorageInfoSync().keys bootstrap 兼容老用户；云端 callFunction 套 try/catch，任何异常 resolve 回退码。
4. **归档合并计数虚高**：`src/utils/archive.js` mergeByTime 重写——localTimes Set 记本地已有 time，`added = capped.length - kept`，远程重复条目与被 cap 截掉的不再计入「新增」。
5. **离开工坊后气泡仍弹出**：`src/pages/lab/lab.vue` 4 个时段 tip 的 setTimeout 收进 `tipTimers[]`，onUnload 统一 clearTimeout。
6. **封存死分支**：lab.vue sealCard 简化为纯渲染（renderCard({stamp:false}) → ensureCardTemp），永不执行的 stamped 历史写入分支删除。
7. **引导浮层永不触发**：lab coachmark 触发从 onReady 挪到 onShow——tabBar 页 onReady 只在创建时跑一次，切 tab 回工坊永远不再触发；onShow 双闸（gu_lab_guided + tut.active）防重复。
8. **防空**：`src/utils/canvas-draw.js` accords 消费点补 `|| []`。
9. **孤儿写键**：`src/utils/tutorial.js` finishTour 删除 `gu_tour_done` 写入（全库无读者）。至此 20 个存储键全部读写配对。

## 三、死代码清理

| 位置 | 内容 | dist 处理 |
|---|---|---|
| src/pages/lab/lab.vue | saveCard 整函数（模板从未调用） | dist 已被编译器 tree-shake，无需补丁 |
| src/pages/lab/lab.vue | formulaText ref + 转换赋值 | dist 留置 K ref 声明（未引用、零行为差） |
| src/pages/home/home.vue | 死 formulaText computed | dist 已删 |
| src/pages/gallery/gallery.vue | GALLERY_LABELS / IMG_EXT 死映射，label(k) 改查 ACCORDS，imgSrc 直接拼 .jpg | dist 留置 `const _` 旧映射（未引用、零行为差） |
| src/pages/notes/ | 空目录删除（页面本体上轮已清） | dist 已无 |

## 四、验证结果

- dist 30 处补丁全部 split/join + 锚点计数守卫命中，逐文件 node --check 通过。
- 沙盒功能测试：favorites 三态契约、archive merge 计数（含一处补丁笔误 `{list:c,added:n}}` 被测试抓出后修正为 `{list:c,added:c.length-n}}`）、wxacode LRU（bootstrap/淘汰/dedupe/unlink/云不可用回退）全部通过。
- 最终 grep 验证：__tips / pyramid / wtrim 各 1 处、gu_tour_done 0 处、formulaText 0 处，recordSeal 死引用已清。
- 临时补丁脚本已删，`src/pages/notes` 空目录已删。

## 五、第三轮复查新发现（已全部修整，2026-09-03 同日完成）

上轮 11 项修复复核全部生效后，续审发现以下问题并已修完（src + dist 双写，dist 4 处补丁全部锚点命中、node --check 全过、14 项沙盒断言全过）：

**P2 两项**

1. **工坊引导蒙层残留**（lab.vue onShow + dist lab.js）：用户带着未关闭的引导蒙层经 tabBar 离开去启动教程，教程结束回工坊时教程遮罩一撤，底下 z300 蒙层露出挡页。修法：onShow 里凡「教程接管中」或「教程已完成（gu_lab_guided 已写）」一律收起蒙层，再走原双闸触发。
2. **submitFeedback check 分支裸暴露**（cloudfunctions/submitFeedback/index.js + src/utils/moderate.js + feedback.vue）：check 分支无频控且前端 cloudModerate 无人调用，等于可无限刷 msgSecCheck 配额的裸接口。修法：删除 check 分支与 action 参数，删除 src 的 cloudModerate 死代码（dist 早已 tree-shake 无需补），feedback.vue 去掉死导入；频控注释同步更新。**注意：此云函数需在微信开发者工具重新上传部署后才生效。**

**P3 各项**

3. feedback.vue 非微信分支未真正提交却记 `feedback_submit` 埋点 → 删除（该分支走条件编译，dist 本就无此代码）。
4. moderate.js 本地词表误杀（「妈的/他妈/你妈/日你」会命中「送给他妈妈」「妈妈的生日你记得吗」）→ 从词表移除并留注释；留言仍有云端 msgSecCheck 兜底，感言只落本地。dist utils/moderate.js 同步删词。
5. progress.js 封存计数写失败后按历史兜底重算，可能重复弹同一层级解锁文案 → 新增 `isabella_tier_shown` 幂等表（存储键第 21 个，读写配对），同一层级文案永远只弹一次；leveledUp 语义不变（lab 门控为 `leveledUp && unlock`，兼容）。dist utils/progress.js 同步。
6. getWxacode 云函数注释写 40001 实判 41001，且 41001（token 失效）换 envVersion 重试无意义 → 改为仅 449 重试，注释修正。
7. card.vue 收藏入参补 `radarMode`（收藏卡雷达模式不再回退默认）+ 过时注释更正；dist card.js 收藏条目同步。
8. lab.vue onReady 重复 `ensureCardTemp()` → src 删除（dist 编译器已折叠成单次调用，无需补丁）。

**验证**：dist 4 处补丁（lab 蒙层 / card radarMode / moderate 词表 / progress 幂等）锚点计数全为 1，node --check 全过；progress 幂等（正常跨层弹一次、写失败重算不重复弹、持续写失败只弹一次）与 moderate 词表（3 组误杀句放行、3 组敏感词仍拦截）共 14 项沙盒断言全过；云函数语法校验通过；临时脚本已清理。

## 六、代码审计 2026-09-03 逐项修订（P1/P2/P3 非云函数部分，src 统一构建，无 dist 手写补丁）

> 依据《代码审计报告-2026-09-03.md》逐项修订。用户明确「云函数不管」：
> P1-1（fail-open）、P2-1（出码鉴权）、P3 云函数错误透传/feedbacks 权限注释属云函数范围，不在本轮。

### P1-2 滑块拖动帧全链路重算 + setData 抖动（lab.vue）
- 拖动帧（@changing，60–100Hz）只更新 `values` 并画雷达（`onSlide` → `drawRadarOnly`）；
- 配方/字幕/雷达 caption/图鉴匹配/挑战计分/卡片重绘全部移到抬手（`onSlideEnd` → `drawLive()`）统一算一次；
- 雷达绘制参数抽成 `paintRadar(radarValues)` 供两条路径共用，保证拖动与抬手画面一致；
- recompute 加 `refreshQuote` 参数：拖动帧不重摇台词，抬手统一换一句（防止跑马灯 + 防抖重绘二次摇台词致卡高抖动）。

### P2-2 wxacode path 超 128 裁剪（src/utils/wxacode.js）
- 注释与实现统一到 **128**（getwxacode 实际限制）：超 ~110 字符上限即丢名字只留 p 参数，绝不静默超限生成失败。

### P2-3 restore 路径忽略 incoming.blend（lab.vue onReady + dist 一并修复）
- 带参直达（扫码/分享）`restoreData` 分支下 `incoming.blend = null` 作废接力——分享配方只存在于撤销栈，不能被更早暂存的「我也调一瓶」顶掉；challenge 不冲突，照常落地。

### P2-4 隐私声明补相册/统计（disclaimer.vue、App.vue）
- 免责声明覆盖 writePhotosAlbum（保存相册授权）与 traceUser（统计），并写清数据用途边界。

### P2-5 历史/收藏列表分批渲染（history.vue、favorites.vue）
- 首屏 20 条 + onReachBottom 每批 20，不再全量 setData；搜索态不限量防命中被藏。

### P2-6 时间戳 isFinite 校验（history.vue、eggs.vue）
- fmt 前加 `isFinite` 闸，损坏/旧版时间戳不再渲染出 `NaN.NaN.NaN`（favorites 已有同款闸）。

### P3 各代码项
1. **onShow 整卡重绘 + 导出瘦身**（lab.vue）：canvas 级 `renderCard` 保留（Canvas2D 残影修复，成本低）；`canvasToTempFilePath` 仅当 `cardTempPath` 缺失才补导出——配方/封存态未变不再每切回一次整卡离屏导出。
2. **archive 导入框限长**（archive.vue）：`maxlength=-1` → `maxlength="50000"`，粘贴超大文本不再无限吃内存。
3. **截断提示一致性**（archive.js + archive.vue + cases-archive.mjs）：mergeByTime 返回 `{list, added, dropped}`——`added` 只算 cap 截断后真正写入的远端新增（满仓不虚报），`dropped` 只算「档案里被本机上限顶掉、没进全」的远端条数（本机旧条目让位不算）；mergeArchive 的 sum 挂 `dropped:{history,favorites}` 分列；archive.vue mergeDone 在 dropped>0 时标题改「收下一部分」并逐行列明「档案里更旧的 N 瓶/件没能并进」，toast 同口径点明「本机已满，部分没能并进」（历史满 50 已有 toast、收藏满 100 card.vue 已有 toast，至此三处截断提示一致）；cases-archive 新增 3 条用例锁定 dropped 语义（全旧满仓 added=0/dropped=3、半仓混合 3 进 2 挤、收藏满 100 dropped 分列与重复不计）。
4. **drawRadarGrow RAF 无取消出口**（canvas-draw.js）：模块级记录当前句柄 + 导出 `cancelRadarGrow()`，连续触发先停上一段；lab.vue onHide 清 sync/share timer + cancelRadarGrow（tabBar 页常驻不销毁，onUnload 只在真退出跑），onUnload 顺带清四个时段气泡定时器。
5. **canvas init 失败无重试**（lab.vue onReady）：首轮 radar/card init 任一失败，隔 400ms 各补一次 initCanvas——onReady 只在 tabBar 页首建跑一次，放弃会让 incoming 接力（图鉴/挑战/分享）永久滞留内存。
6. **applyIncomingIfReady blend 提前 return**（lab.vue）：去掉 blend 分支提前 return，blend 与 challenge 各自独立落地——极端同帧构造下 challenge 不再顺延到下次进工坊（正常场景不同帧，行为不变）。
7. **manifest app-plus 高危权限**（src/manifest.json）：移除 14 个与小程序无关的 Android 权限（CAMERA/GET_ACCOUNTS/READ_PHONE_STATE 等）+ SC 特性与 uses-feature，收敛为最小集（INTERNET/网络状态/CHANGE_WIFI/VIBRATE），防未来误打 App 包；注释标明勿恢复。
8. **project.config.json urlCheck**：`false` → `true`——本项目无真实外部请求（云函数免校验、图片本地化），开启后上传即可暴露外链问题。

### 验证
- `npm run audit` 全绿：5 个 audit 脚本 + 功能测试 103 条（含 archive dropped 新增 3 条）；audit-selectors 16 条提醒均为已知误报（exit 0）。
- `npm run build:mp-weixin` 通过，`rm-pill` 在 app.wxss 恰 1 处，主包 1017K ≤ 2MB；
- 构建后清理脚本偶发 trash 失败（genie-trash 无法整目录回收），已手工逐文件删净残留 `dist/build/mp-weixin/cloudfunctions`，重跑构建 DONE 全绿。
- 云函数 getWxacode/submitFeedback 目录下的未提交改动属用户另行安排的部署内容，不在本次提交范围内时说明即可。
