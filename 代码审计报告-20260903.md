# 代码审计报告 · 2026-09-03

> 范围：E:\Desk\new-web\isabella-mini（「古先生的调香日记」微信小程序，uni-app Vue3）
> 方法：三路并行审计（安全与平台合规 / 性能与资源 / 鲁棒性与 src↔dist 一致性），
> 关键发现均已在源码层人工复核；dist 产物用 terser 反混淆后逐函数比对。
> 分级：P1 高危 / P2 中 / P3 建议。标记【已复核】= 本人复核确认。

---

## P1（2 项）

### P1-1 留言内容安全 fail-open（平台合规红线）【已复核】
- 位置：`cloudfunctions/submitFeedback/index.js:31-38`
- 问题：msgSecCheck 的非 87014 异常（45009 限频、网络抖动等）走 fail-open——`{ pass: true, needReview: true, checkError }` 直接写库。攻击者可先刷消息接口触发限频，再提交违规内容，留言未过官方审核即入库，违反内容安全要求。
- 修复建议：异常时 fail-closed（返回「稍后重试」不写库），或改落「待审」独立集合；若坚持放行，控制台需按 checkError 标记定期过滤复核。需重新部署云函数。

### P1-2 滑块拖动帧全链路重算 + setData 抖动【已复核】
- 位置：`src/pages/lab/lab.vue:1096-1106`（onSlide → drawLive → recompute）
- 问题：`@changing` 60–100Hz 每帧执行 generateFormula（104 味全量打分）+ generatePyramid + radarSummary + 图鉴余弦遍历 + findExactMatch + 挑战计分，并连写 formulaParts/formulaPyramid/radarCaption/nearPerfume/challengeScore 六个 ref → 每帧 setData、整页重渲染（含滑块内联事件重建）。低端机拖到明显掉帧。
- 修复建议：拖动帧只更新 `values` 并画雷达；配方/文案/字幕计算推迟到 onSlideEnd（抬手统一算一次）。400ms 卡片防抖与 1.5s 分享图防抖设计正确，不动。

---

## P2（6 项）

| # | 位置 | 问题 | 建议 |
|---|---|---|---|
| 1 | cloudfunctions/getWxacode/index.js:27-37 | 无身份鉴权/无限频，可脚本刷爆 10 万/月出码额度 | 按 openid 加频控；path 白名单前缀校验 `pages/card/card?` |
| 2 | src/utils/wxacode.js:59-67 | 注释写「上限 1024/超 1000 才丢名」而 getwxacode 的 path 实际限 **128 字符**：长香名会静默生成失败→回退占位图 | 按 128 重新裁剪：带上名字超 ~110 就丢弃名字只留 p |
| 3 | src/pages/lab/lab.vue:1839-1850 | 冷启动分享/扫码进入先 applyRestore，再无条件 applyIncomingIfReady——card 页遗留的「我也调一瓶」incoming.blend 会顶掉分享来的配方（只存在于撤销栈） | restoreData 路径下忽略 incoming.blend（challenge 仍照常落地） |
| 4 | src/pages/disclaimer/disclaimer.vue:20-22 + card.vue:393 + App.vue:11 | 隐私声明只列 openid+内容，未覆盖相册权限（writePhotosAlbum）与统计（traceUser）；真机存图授权可能被拦截 | 免责声明补两项；微信后台《用户隐私保护指引》同步声明；保存相册 fail 分支引导 |
| 5 | src/pages/history/history.vue:165-174、favorites.vue:182-184 | 列表全量 setData（50/100 行×3 柱）无分批 | 与 gallery 一致按 24/批 onReachBottom 展开 |
| 6 | src/pages/eggs/eggs.vue:43-47、history.vue:63/77 | 损坏/旧版时间戳 → new Date → NaN.NaN.NaN（favorites 已有 isSealedTime 过滤，这两处无） | fmt 前加 isFinite 校验 |

---

## P3（建议记档）

- onShow 无条件整卡重绘 + canvasToTempFilePath 导出（lab.vue:1799-1803）：配方/封存态未变时跳过。
- archive.vue:35-41 导入 textarea `maxlength=-1` 无上限：粘贴超大文本卡顿，建议限长。
- card.vue:125-151 分享参数 s/q/n/dn/dt 无长度/字段上限（try/catch 兜底，画布层有截断，低风险）。
- 截断提示不一致：历史满 50 有 toast，收藏 100 与归档合并均静默。
- drawRadarGrow 的 RAF 无 cancel（canvas-draw.js:364-376）；onHide 建议兜底清 sync/share 定时器。
- 云函数错误透传（errCode/errMsg 原样回）：建议统一归为「生成失败，请重试」。
- feedbacks 集合权限只写在注释中：部署清单固化「仅创建者可读写」。
- sealCore 副作用先行（lab.vue:1419 注释说明）为**有意设计**：画卡失败重封会虚增 seal_count/todaySeal——需默认接受的取舍，如可改则把 bump 移画卡成功之后。
- applyIncomingIfReady 的 blend 分支提前 return：极端构造下 challenge 顺延到下次进工坊（不丢）。
- canvas init 失败时 incoming 永滞、tabBar 不复建无重试：init 失败给一次重试。
- manifest.json:25-41 app-plus 高危权限声明与小程序无关（会导致未来 App 包）；project.config.json urlCheck:false 建议上线前改 true。

---

## 确认无问题（一句话结论）

- 安全：openid 全部取自服务端 getWXContext，无客户端伪造；无 db.collection 客户端直读；无硬编码密钥；msgSecCheck 三态+87014 正确；无图片上传；无支付/赞赏/诱导分享文案；analytics 无 PII。
- 合规：微信授权仅在用户点「保存到相册」时触发；感言仅落本地（lab.vue:1619/1642），留言云写入前过审（feedback.vue:89）。
- 鲁棒：初始化链路、card 深链竞态、归档幂等同步、时区边界（streak 跨 0 点正确）、存储损坏防护（try/catch+Array.isArray+字段兜底）、decode 参数防脏、除零/NaN 防护 全链路 OK。
- 一致性：dist 与 src 逐函数一致（含从未手改过的 community/eggs/tutorial）；包体 1.0MB 未超限，暂无需分包；Canvas2D 统一、drawImage 不每帧、rarity O(1)、无热路径重复计算；失败路径均有兜底（FALLBACK_QR/虚线占位/toast）。

---

## 建议处理顺序

1. 先修两个 P1（云函数 fail-closed + 滑块抬手计算）—— 合规与体验各一项硬伤。
2. P2 的 2/3/6 是小改（各 ~2 行），可与 P1 同批。
3. P2 的 1/4/5 需决策（云函数频控方案、隐私声明属于后台操作、列表分批牵涉模板）。